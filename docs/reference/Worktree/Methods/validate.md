# validate

Validate that the worktree is in a valid state.

## Signature

```ts
class Worktree {
  validate(): void;
}
```

### Errors

<ul class="param-ul">
  <li class="param-li param-li-root">
    <span class="param-type">Error</span>
    <br>
    <p class="param-description">Throws error if the worktree is in an invalid state.</p>
  </li>
</ul>