# isEmpty

Check if the reflog is empty.

## Signature

```ts
class Reflog {
  isEmpty(): boolean;
}
```

### Returns

<ul class="param-ul">
  <li class="param-li param-li-root">
    <span class="param-type">boolean</span>
    <br>
    <p class="param-description"><code>true</code>  if the reflog is empty,  <code>false</code>  otherwise.</p>
  </li>
</ul>