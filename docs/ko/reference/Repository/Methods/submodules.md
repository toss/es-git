# submodules

이 리포지토리의 모든 서브모듈을 로드하고 반환해요.

## 시그니처

```ts
class Submodule {
  submodules(): Submodule[];
}
```

### 반환 값

<ul class="param-ul">
  <li class="param-li param-li-root">
    <span class="param-type">Submodule[]</span>
    <br>
    <p class="param-description">이 리포지토리에 존재하는 모든 서브모듈 목록</p>
  </li>
</ul>