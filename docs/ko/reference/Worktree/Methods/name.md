# name

이 워크트리의 이름을 가져와요.

## 시그니처

```ts
class Worktree {
  name(): string | null;
}
```

### 반환 값

<ul class="param-ul">
  <li class="param-li param-li-root">
    <span class="param-type">string | null</span>
    <br>
    <p class="param-description">이 워크트리의 이름. 워크트리에 이름이 없으면 <code>null</code>을 반환해요.</p>
  </li>
</ul>