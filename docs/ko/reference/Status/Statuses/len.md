# len

이 목록의 상태 항목 수를 가져와요.

## 시그니처

```ts
class Statuses {
  len(): number;
}
```

### 반환 값

<ul class="param-ul">
  <li class="param-li param-li-root">
    <span class="param-type">bigint</span>
    <br>
    <p class="param-description">상태 변경 사항이 없으면 (상태 목록이 생성될 때 지정된 옵션에 따라) 0을 반환해요.</p>
  </li>
</ul>